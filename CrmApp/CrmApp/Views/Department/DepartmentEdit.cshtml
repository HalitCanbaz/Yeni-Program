@model DepartmentEditViewModel


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
        }

<div class="card">
    <div class="card-body">
        <div class="row">
            <div class="col-md-4">
                <h5>Departman Adı Düzelt</h5>
                <hr />

                <form asp-controller="Department" asp-action="DepartmentEdit">
                    @if (TempData["messageError"] != null)
                    {
                        <div class="alert alert-icon alert-danger" role="alert">
                            @TempData["messageError"]
                        </div>
                    }
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="DepartmentName" class="control-label">Kategori Kodu</label>
                        <input asp-for="DepartmentName" class="form-control" />
                        <span asp-validation-for="DepartmentName" class="text-danger"></span>
                    </div>                    
                    <div class="form-group">
                        <input type="submit" value="Düzelt" class="btn btn-secondary" />
                    </div>
                </form>
            </div>
        </div>

        <div>
            <a asp-action="DepartmentList" class="btn btn-info">Departman Listesi</a>
        </div>
    </div>
</div>

